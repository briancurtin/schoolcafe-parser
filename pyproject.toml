[project]
name = "schoolcafe-parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Brian Curtin", email = "brian.curtin@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pre-commit>=4.6.2",
    "pypdf>=6.16.1",
]

[project.scripts]
schoolcafe-parser = "schoolcafe_parser:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
